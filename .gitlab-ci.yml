stages:
  - build
  - deploy

build:
  stage: build
  image: maven:3.9.8-eclipse-temurin-17
  variables:
    MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"
    # 🔴 关键：用环境变量传递不安全Registry，完全绕开daemon.json
    DOCKER_BUILDKIT: 0
    DOCKER_OPTS: "--insecure-registry 49.233.185.174:5000 --insecure-registry article-gitlab:5000"
  before_script:
    - apt update && apt install -y docker.io
  script:
    - cd backend
    - mvn clean package -DskipTests
    # 🔴 关键：登录时直接指定--insecure-registry（注意位置）
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin 49.233.185.174:5000
    # 🔴 关键：构建和推送时添加--insecure-registry参数
    - docker build --insecure-registry 49.233.185.174:5000 -t 49.233.185.174:5000/fds/java-lala:latest .
    - docker push --insecure-registry 49.233.185.174:5000 49.233.185.174:5000/fds/java-lala:latest
  only:
    - feature/initial-commit
  tags:
    - docker
  cache:
    paths:
      - .m2/repository/

deploy:
  stage: deploy
  image: ubuntu:22.04
  before_script:
    - apt update && apt install -y docker.io docker-compose openssh-client
    - mkdir -p ~/.ssh
    - echo "$DEPLOY_SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_ed25519
    - chmod 600 ~/.ssh/id_ed25519
    - ssh-keyscan 49.233.185.174 >> ~/.ssh/known_hosts
  script:
    - ssh root@49.233.185.174 "mkdir -p /home/ubuntu/article-fds"
    - scp docker-compose.yml root@49.233.185.174:/home/ubuntu/article-fds/
    - ssh root@49.233.185.174 "cd /home/ubuntu/article-fds && docker-compose up -d"
  only:
    - feature/initial-commit
  tags:
    - docker
